# Nonprofit Data Scraper Agent — Python 3.11+
anthropic>=1.7.0,<2        # Claude API client (1.x; resolved to 1.7.0 here)
pydantic>=2.7.0            # schema definition + validation
httpx>=0.27.0              # HTTP with timeouts
beautifulsoup4>=4.12.0     # link extraction, sitemap/RSS parsing
lxml>=5.2.0                # parser backend for BeautifulSoup
trafilatura>=1.9.0         # clean main-text extraction from HTML
pymupdf>=1.24.0            # PDF text extraction
playwright>=1.44.0         # JS-render fallback; needs `playwright install chromium`
rapidfuzz>=3.9.0           # fuzzy name matching (URL verification, ProPublica)
python-dotenv>=1.0.0       # load ANTHROPIC_API_KEY from .env
